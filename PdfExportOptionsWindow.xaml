<Window x:Class="HGC.PdfExportOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Width="520"
        Height="650"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="{DynamicResource ContentBackgroundBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">

    <Window.Resources>
        <Style x:Key="HgcDialogButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource AppAccentBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppAccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                                CornerRadius="7"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}">
                            <Grid>
                                <Border CornerRadius="7" Opacity="0.35">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#88FFFFFF" Offset="0"/>
                                            <GradientStop Color="#22FFFFFF" Offset="0.45"/>
                                            <GradientStop Color="#33000000" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                </Border>

                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}"
                                                  RecognizesAccessKey="True"/>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.86"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.70"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HgcCloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#C42B1C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CloseBorder"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CloseBorder"
                                        Property="Background"
                                        Value="#E81123"/>
                            </Trigger>

                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CloseBorder"
                                        Property="Background"
                                        Value="#8B1A12"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,18,0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36"
                            ResizeBorderThickness="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="{DynamicResource AppAccentBrush}"
            BorderThickness="2"
            Background="{DynamicResource ContentBackgroundBrush}">

        <Grid Background="{DynamicResource ContentBackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="72"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{DynamicResource TitleBarBrush}">
                <Grid>
                    <TextBlock x:Name="TitleText"
                               Text="Eksport PDF"
                               VerticalAlignment="Center"
                               Margin="12,0,0,0"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource AppTextBrush}"/>

                    <Button Content="X"
                            Style="{StaticResource HgcCloseButtonStyle}"
                            HorizontalAlignment="Right"
                            Click="CancelButton_Click"/>
                </Grid>
            </Border>

            <Border Grid.Row="1"
                    Margin="20,20,20,0"
                    Padding="18"
                    Background="{DynamicResource PreviewBoxBrush}"
                    BorderBrush="{DynamicResource AppBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="10">

                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel>
                        <TextBlock x:Name="SubtitleText"
                                   Text="Wybierz elementy raportu"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource AppTextBrush}"
                                   Margin="0,0,0,14"/>

                        <CheckBox x:Name="IncludeTitlePageCheckBox"
                                  Content="Strona tytułowa"
                                  IsChecked="True"/>

                        <CheckBox x:Name="IncludeStatisticsCheckBox"
                                  Content="Statystyki"
                                  IsChecked="True"/>

                        <CheckBox x:Name="IncludeGamesCheckBox"
                                  Content="Gry"
                                  IsChecked="True"/>

                        <CheckBox x:Name="IncludeHardwareCheckBox"
                                  Content="Sprzęt"
                                  IsChecked="True"/>

                        <CheckBox x:Name="IncludePcCheckBox"
                                  Content="Komputery"
                                  IsChecked="True"/>

                        <CheckBox x:Name="IncludeAccessoriesCheckBox"
                                  Content="Akcesoria"
                                  IsChecked="True"/>

                        <CheckBox x:Name="IncludeServiceCheckBox"
                                  Content="Historia serwisu"
                                  IsChecked="True"/>

                        <CheckBox x:Name="IncludeCurrencyRatesCheckBox"
                                  Content="Kursy walut"
                                  IsChecked="True"/>

                        <CheckBox x:Name="IncludePhotosCheckBox"
                                  Content="Dodaj zdjęcia główne"
                                  IsChecked="True"/>

                        <CheckBox x:Name="StartSectionsOnNewPageCheckBox"
                                  Content="Rozpoczynaj każdą sekcję od nowej strony"
                                  IsChecked="True"/>

                        <CheckBox x:Name="NumberPagesCheckBox"
                                  Content="Numeruj strony"
                                  IsChecked="True"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">

                <Button x:Name="GenerateButton"
                        Content="Generuj"
                        Style="{StaticResource HgcDialogButtonStyle}"
                        Margin="5,0"
                        Click="GenerateButton_Click"/>

                <Button x:Name="CancelButton"
                        Content="Anuluj"
                        Style="{StaticResource HgcDialogButtonStyle}"
                        Margin="5,0"
                        Click="CancelButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>